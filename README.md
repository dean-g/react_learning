Client-based app/game (Props, State (hooks))

Animation library: motion one

React Data Tables
Network requests: TanStack Query (easier variation: useSWR(Vercel))
Immer
Redux (for complicated client state, time-travel debug feature)
useReducer (it has many Redux features)
RadixUI (For accessible components)
Styled components/Emotion

NextJs Middleware ()

Express Backend

Client-side:
React Context for authentication
React Router for routing


Explorer App (Props, State, Context, TanStack Query + fetch API, Existing API)

Enterprise App (Props, State, Context, TanStack Query + fetch API, Supabase (Backend As A Service))

